# Moa 서비스마감 스크래퍼 필수 패키지
selenium>=4.15.0
undetected-chromedriver>=3.5.5   # Cloudflare 봇 챌린지("Just a moment") 회피 — xvfb non-headless 권장
python-dotenv>=1.0.0
requests>=2.34.0   # 2.34부터 chardet<8 허용 — 2.32는 chardet 7에 호환 경고를 stderr로 뱉는다
openpyxl>=3.1.0
msoffcrypto-tool>=5.0.0   # 암호화(CDFV2) 엑셀 다운로드 복호 — SmileEDI 동일
