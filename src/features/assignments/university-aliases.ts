/**
 * 대학명 별칭 — 원천이 부르는 이름을 **원장 이름**으로 옮긴다.
 *
 * 설계 F1 은 *추측해서* 잇지 말라는 것이다. 여기 줄들은 전부 실측으로 확인했고
 * (2026-09-21, 19/19 대상이 원장에 그 업무종류로 존재), 줄마다 왜 그렇게 읽는지가
 * 옆에 적혀 있다. 기계가 고른 것이 아니라 사람이 고른 것이다.
 *
 * **원장이 정본이다.** 마감(`closing_services`)과 발표(`announcement_services`)가
 * 미러라, 미러 쪽 표기를 원장에 맞춘다. 반대로 하면 엑셀을 고쳐야 한다.
 *
 * 세 갈래가 있다.
 *
 * | 갈래 | 예 |
 * |---|---|
 * | 캠퍼스가 원장에만 적혀 있다 | `한양대학교 대학원` → `한양대학교(서울)` |
 * | 전형·부속기관이 이름에 섞였다 | `서울여자대학교 외국인` → `서울여자대학교` |
 * | 표기·교명이 다르다 | `인하공업전문대학` → `인하공업전문대학교` |
 *
 * **표(DB)가 아니라 커밋된 상수인 이유**: 교명 변경·신규 전형처럼 연 몇 회 바뀌는
 * 값이라 마이그레이션·RLS·관리 화면을 세울 만큼 자주 움직이지 않고, **새 갈림은
 * 배분현황 배너가 알려 준다**. 30개를 넘거나 배포 없이 고쳐야 하면 그때 표로 올린다.
 *
 * 키가 `이름|업무종류` 인 것은 같은 이름이 업무마다 다른 곳을 가리키기 때문이다 —
 * 발표의 `동국대학교` 는 (서울)이지만, 다른 원천에서 같은 이름이 오면 어느 캠퍼스인지
 * 알 수 없어 손대면 안 된다.
 */
export const UNIVERSITY_ALIASES: Readonly<Record<string, string>> = {
  // ── 마감(원서접수·대학원) ──────────────────────────────────────────────
  /** 원장 대학원 칸이 `(서울)` 하나뿐이다 — ERICA 는 원서접수·상담앱만 있다. */
  "한양대학교 대학원|대학원": "한양대학교(서울)",
  /** 원장 대학원 칸이 `(서울)` 하나뿐이다. */
  "연세대학교 대학원|대학원": "연세대학교(서울)",
  /** 원장은 `부설` 없이 적는다. */
  "숭의여자대학교 부설 평생교육원|원서접수": "숭의여자대학교 평생교육원",
  /** 원장은 `대학교`, 마감은 `대학`. 같은 학교다. */
  "인하공업전문대학|원서접수": "인하공업전문대학교",
  /** 공백 하나 차이. */
  "광주교육대학교광주부설초등학교|원서접수": "광주교육대학교 광주부설초등학교",
  /** 경연대회도 서경대 원서접수 담당이 본다 — 원장에 별도 칸이 없다. */
  "서경대학교 경연대회|원서접수": "서경대학교",
  /**
   * 원장은 중·고를 나눠 두고 마감은 합쳐 부른다(사용자 확인).
   *
   * **오는 서비스가 `중학교 1단계` 하나라서** 중학교로 잇는다. 고등학교 건이
   * 같은 이름으로 오기 시작하면 이 줄을 다시 봐야 한다 — 그때는 1건이 조용히
   * 중학교 담당자에게 붙는다.
   */
  "청심국제중고등학교|원서접수": "청심국제중학교",
  /** 마감은 제도 이름(`독학학위제`)으로, 원장은 운영 기관으로 적는다. */
  "독학학위제|원서접수": "평생교육진흥원(독학사)",

  // ── 발표(PIMS) ────────────────────────────────────────────────────────
  // 발표 자료는 붙여넣기 업로드라 대학명이 자유 텍스트다. 전형 구분까지 이름에
  // 섞여 들어와서 규칙 하나로 못 자른다.
  /** 원장 PIMS 칸은 `세종대학교` 하나 — 대학원 발표도 같은 담당이다. */
  "세종대학교 대학원|PIMS": "세종대학교",
  /** 국제교류처는 부서명이다. */
  "가천대학교 국제교류처|PIMS": "가천대학교",
  /** `외국인` 은 전형 구분 — 원장 PIMS 는 전형으로 나누지 않는다. */
  "서울여자대학교 외국인|PIMS": "서울여자대학교",
  "성신여자대학교 외국인|PIMS": "성신여자대학교",
  "이화여자대학교 외국인|PIMS": "이화여자대학교",
  /** 원장은 괄호로 캠퍼스를 적는다. */
  "동국대학교 WISE캠퍼스|PIMS": "동국대학교(WISE)",
  "건국대학교 글로컬|PIMS": "건국대학교(글로컬)",
  /** 캠퍼스가 안 적힌 것은 (서울)이다 — WISE 는 따로 온다(위 줄). */
  "동국대학교|PIMS": "동국대학교(서울)",
  /** 2024 교명 변경: 한경대학교 → 한경국립대학교. 발표 자료가 옛 이름이다. */
  "한경대학교|PIMS": "한경국립대학교",
  /** 원장은 `대학`, 발표는 `대학교`. */
  "경찰대학교|PIMS": "경찰대학",
  /** `-단계` 는 발표 회차 표기다. */
  "한국항공대학교-단계|PIMS": "한국항공대학교",
};

/**
 * 그 이름·업무종류의 원장 이름. 없으면 `null` — **모르면 손대지 않는다.**
 */
export function aliasOfUniversity(
  universityName: string,
  workKind: string,
): string | null {
  return UNIVERSITY_ALIASES[`${universityName}|${workKind}`] ?? null;
}
