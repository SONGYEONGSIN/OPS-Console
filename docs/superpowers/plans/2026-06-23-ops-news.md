# 운영부 뉴스 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to execute this plan. 각 Task를 독립 subagent에 위임하되, Task 간 의존 순서(아래 명시)를 지킨다. 모든 코드 Task는 RED→GREEN→REFACTOR(TDD Iron Law)를 강제하고, 각 Step의 "기대 출력"을 실제 명령 실행 결과로 검증한 뒤에만 다음 Step으로 진행한다. TDD 예외(마이그/스타일/타입정의/페이지 SSR)는 §rules/tdd.md 기준으로만 적용하고, 예외라도 검증(빌드/렌더/typecheck)은 필수다.

## Goal

OPS-Console '개요' 그룹의 **운영부 달력(`schedule`) 직후**에 **운영부 뉴스(`news`)** 메뉴를 추가한다.
대학 관련 뉴스(통폐합·폐교·정원감축·글로컬대학·구조조정)를 멀티소스 RSS로 자동 수집해 목록형(ListPattern + 인스펙터 읽기전용)으로 보여준다.

- 키워드 기반 구글 뉴스 RSS(핵심) + 교육부/전문지 직접 피드(보강)를 주기 수집
- 운영부 뉴스 메뉴에서 제목·출처·날짜 확인, 클릭 시 원문 링크
- 자동 주기 수집 (cron-job.org → `/api/automations/run?jobId=news-collect`)

근거 스펙: `docs/superpowers/specs/2026-06-23-ops-news-design.md`. 본 기능은 기존 `insights-collect`(YouTube 수집) 자동화 패턴의 복제다.

## Architecture

```
[cron-job.org → POST /api/automations/run?jobId=news-collect (Bearer CRON_SECRET)]
  registry.ts: { id:"news-collect", run: runNewsCollect } (1줄 등록 — cron route/이력/수동실행 UI 자동)
  runNewsCollect():
    1. NEWS_SOURCES 순회 (키워드 5개 구글뉴스RSS URL + 교육부/전문지 placeholder)
    2. 각 URL fetch() → XML 문자열
    3. 순수 변환: parseRssItems(fast-xml-parser) → mapRssItemsToNews → dedupeByLink
       (rfc2822ToIso / stripHtml 보조 순수 함수)
    4. createAdminClient().from("news").upsert(rows, { onConflict:"link" })  (service_role write)
    5. 60일(CLEANUP_DAYS) 지난 행 delete. 소스 1개 실패해도 계속 (errors[] 누적)
    → AutomationRunResult 반환
        ▼ (페이지 autoRefresh)
[Vercel 웹앱 /dashboard/news — SSR]
  requireMenu("news") → listNews() (authenticated RLS, published_at desc, zod per-row)
  → newsRowToListRow → ListPattern variant="news" (읽기전용 인스펙터: 제목·출처·날짜·요약 + 원문 링크)
```

권한 경계: `news` 테이블 쓰기는 service_role(잡)만, read는 authenticated 전원(운영부 공개) — `insight_videos` RLS 패턴 동일. 웹앱은 DB만 읽는다.

## Tech Stack

- Next.js App Router(RSC) / TypeScript / Tailwind 4(디자인 토큰)
- Supabase(@supabase/ssr server client, admin service_role) + zod
- Vitest(unit) — 순수 함수 + fetch mock 패턴(`insights-collect.test.ts` 차용)
- 신규 의존성: `fast-xml-parser` (RSS XML 파싱). 그 외 글로벌 `fetch`만 사용
- 자동화 인프라 재사용: cron route(`/api/automations/run`), `automation_runs` 이력, 수동실행 UI, `getJob`, `AutomationJob` 타입, `AutomationRunResult`

재사용 자산(실측):
- 잡 5단계 골격 + errors[] 누적 + admin upsert + cleanup: `src/features/automations/jobs/insights-collect.ts`
- 순수 함수 단위테스트 컨벤션: `src/features/automations/jobs/__tests__/insights-collect.test.ts`
- registry 1줄 등록: `src/features/automations/registry.ts` (AutomationJob 타입은 `./types`)
- 테이블/RLS/GRANT/PostgREST reload: `supabase/migrations/20260518_insight_videos_table.sql`, `20260518b_insight_videos_rls.sql`
- zod row schema + SSR list query(authenticated RLS): `src/features/insight-videos/{schemas,queries}.ts`
- View-only variant 템플릿: `.../list-variants/worklog/View.tsx` + 테이블 템플릿 `.../mailbox/Table.tsx`
- variant registry/types: `.../inspector/list-variants/registry.ts`, `types.ts`
- ListRow 옵셔널 필드 확장 패턴: `.../patterns/ListPattern.tsx` (worklog*/mail* 블록)
- 페이지 SSR(list variant) + row-mapper + autoRefresh: `src/app/dashboard/mailbox/page.tsx` + `_row-mapper.test.ts`
- 사이드바: `src/app/dashboard/_data.ts` '개요' group(line 30~50, schedule item line 42~49)
- 페이지 메타: `src/app/dashboard/_data/page-meta-config.ts`

---

## Task 1 — 의존성 + Supabase 마이그레이션 (news 테이블 + RLS + GRANT)

스펙 §5,§7. TDD 예외(.json/.sql) — 검증은 설치 성공 + SQL dry parse로 대체. **DB 적용은 메모리 `db-migration-apply` 절차**(Supabase CLI 없음 — DATABASE_URL 풀러 + `pg` 인라인)를 따른다. 본 Task는 설치 + 파일 작성까지 범위로 하고, 적용은 운영 단계에서 수행.

### Files
- Modify `package.json` / `package-lock.json` (`npm i fast-xml-parser`)
- Create `supabase/migrations/20260623_news_table.sql`
- Create `supabase/migrations/20260623b_news_rls.sql`

### Steps

- [ ] **Step 1: fast-xml-parser 설치** — RSS XML 파싱 라이브러리.
  - 명령: `npm i fast-xml-parser`
  - 기대 출력: `added 1 package` (또는 이미 존재 시 `up to date`). `node -e "require('fast-xml-parser')"` → 에러 없음(exit 0).

- [ ] **Step 2: 테이블 마이그 작성** — `20260623_news_table.sql`. `insight_videos_table.sql` 구조 그대로 차용(link unique = dedupe 키). published_at desc + keyword 인덱스.

```sql
-- 운영부 뉴스 (news) 도메인 — 대학 관련 뉴스 멀티소스 RSS 자동 수집
-- 사이드바: '개요' > '운영부 뉴스' (slug `news`, pattern `list`)
-- 정책 핵심: 운영부 전체 read / 쓰기는 service_role 전용 (news-collect 잡)
-- 쓰기 RLS는 별도 20260623b — INSERT/UPDATE/DELETE 정책 없음 → authenticated 차단

begin;

------------------------------------------------------------
-- 1) 테이블
------------------------------------------------------------

create table if not exists public.news (
  id            uuid primary key default uuid_generate_v4(),
  link          text not null unique,        -- 원문(또는 구글 리다이렉트) URL — dedupe 키
  title         text not null,               -- 기사 제목
  source        text,                         -- 언론사명 (RSS <source>에서 추출)
  published_at  timestamptz,                  -- 기사 게시일 (RFC2822 → ISO 변환 후 저장)
  summary       text,                         -- description HTML 제거 스니펫
  keyword       text,                         -- 수집에 사용된 검색 키워드
  collected_at  timestamptz not null default now()
);

------------------------------------------------------------
-- 2) 인덱스
------------------------------------------------------------

-- link는 unique 제약으로 자동 인덱스 생성됨
create index if not exists news_published_at_desc_idx
  on public.news (published_at desc);

create index if not exists news_keyword_idx
  on public.news (keyword);

commit;

------------------------------------------------------------
-- 3) PostgREST schema cache reload
------------------------------------------------------------

notify pgrst, 'reload schema';

-- 검증 (수동):
-- \d public.news
-- 기대: 7 컬럼 (id, link, title, source, published_at, summary, keyword, collected_at)
-- select indexname from pg_indexes where tablename='news' order by 1;
-- 기대: 2 user index + 1 pkey + 1 unique (link)
```

- [ ] **Step 3: RLS + GRANT 마이그 작성** — `20260623b_news_rls.sql`. `insight_videos_rls.sql` 복사 — SELECT authenticated 전원, INSERT/UPDATE/DELETE 정책 없음(service_role write), GRANT 명시(42501 함정 회피).

```sql
-- news 테이블 RLS + GRANT
-- 정책: SELECT 전원 read / INSERT·UPDATE·DELETE 정책 없음 → authenticated 차단
-- 쓰기는 service_role (RLS bypass)만 가능 — news-collect 잡이 사용

begin;

------------------------------------------------------------
-- 1) RLS enable
------------------------------------------------------------

alter table public.news enable row level security;

------------------------------------------------------------
-- 2) select — 운영부 전체 read
------------------------------------------------------------

drop policy if exists "news_select_all" on public.news;
create policy "news_select_all"
  on public.news for select
  to authenticated
  using (true);

-- INSERT/UPDATE/DELETE 정책 없음 — authenticated는 RLS로 자동 차단
-- service_role은 RLS bypass라 news-collect 잡이 그대로 쓰기 가능

------------------------------------------------------------
-- 3) GRANT (학습된 함정 — 42501)
------------------------------------------------------------

grant select on public.news to authenticated;
grant all on public.news to service_role;

commit;

------------------------------------------------------------
-- 4) PostgREST cache reload
------------------------------------------------------------

notify pgrst, 'reload schema';

-- 검증 (수동):
-- select policyname, cmd from pg_policies where tablename = 'news';
-- 기대: news_select_all (1개)
-- select has_table_privilege('authenticated', 'public.news', 'SELECT');  -- 기대: t
-- select has_table_privilege('authenticated', 'public.news', 'INSERT');  -- 기대: f
```

- [ ] **Step 4: 검증** — 설치 성공 + 파일 2개 생성 확인. SQL 적용은 운영 단계에서 메모리 `db-migration-apply` 절차로 수행.
  - 명령: `ls supabase/migrations/20260623*.sql`
  - 기대 출력: `20260623_news_table.sql`, `20260623b_news_rls.sql` 2개. 적용 시 위 마이그 주석의 수동 검증 쿼리로 RLS 확인.

- [ ] **Step 5: 커밋** — `git add package.json package-lock.json supabase/migrations/20260623_news_table.sql supabase/migrations/20260623b_news_rls.sql && git commit -m "feat(news): fast-xml-parser 의존성 + news 테이블/RLS 마이그"`

**의존**: 없음

---

## Task 2 — RSS 파싱 순수 함수 + NEWS_SOURCES (RED 먼저)

스펙 §3,§4. **TDD 핵심 Task.** 외부 I/O 없는 순수 함수(파싱/정규화/dedupe)를 export해 고정 XML 픽스처로 단위테스트한다. `insights-collect.test.ts`의 순수 함수 테스트 컨벤션 차용. RED→GREEN.

### Files
- Create `src/features/automations/jobs/__tests__/news-sources.test.ts`
- Create `src/features/automations/jobs/news-sources.ts`

### Steps

- [ ] **Step 1: RED — 순수 함수 테스트 작성** — `news-sources.test.ts`. 고정 XML 픽스처(구글 뉴스 RSS 형태)로 parse→map→dedupe + 보조 함수(rfc2822ToIso/stripHtml) 검증.

```ts
import { describe, it, expect } from "vitest";
import {
  NEWS_SOURCES,
  buildGoogleNewsRssUrl,
  parseRssItems,
  mapRssItemsToNews,
  dedupeByLink,
  rfc2822ToIso,
  stripHtml,
  type RssItem,
} from "../news-sources";

const FIXTURE_XML = `<?xml version="1.0"?>
<rss version="2.0"><channel>
  <item>
    <title>A대학 B대학 통폐합 추진</title>
    <link>https://news.google.com/rss/articles/AAA?oc=5</link>
    <pubDate>Tue, 16 Jun 2026 07:00:00 GMT</pubDate>
    <description>&lt;a href="x"&gt;A대학 B대학 통폐합 추진&lt;/a&gt;&amp;nbsp;한국대학신문</description>
    <source url="https://news.unn.net">한국대학신문</source>
  </item>
  <item>
    <title>C대학 정원감축 확정</title>
    <link>https://news.google.com/rss/articles/BBB?oc=5</link>
    <pubDate>Mon, 15 Jun 2026 02:30:00 GMT</pubDate>
    <description>본문 스니펫</description>
    <source url="https://kyosu.net">교수신문</source>
  </item>
</channel></rss>`;

describe("NEWS_SOURCES", () => {
  it("키워드 5개 구글 뉴스 소스 포함", () => {
    const keywords = NEWS_SOURCES.filter((s) => s.kind === "google").map(
      (s) => s.keyword,
    );
    expect(keywords).toEqual(
      expect.arrayContaining(["통폐합", "폐교", "정원감축", "글로컬대학", "구조조정"]),
    );
  });
});

describe("buildGoogleNewsRssUrl", () => {
  it("키워드를 인코딩한 구글 뉴스 RSS URL 생성", () => {
    const url = buildGoogleNewsRssUrl("대학 통폐합");
    expect(url).toContain("news.google.com/rss/search");
    expect(url).toContain(encodeURIComponent("대학 통폐합"));
    expect(url).toContain("hl=ko");
    expect(url).toContain("ceid=KR:ko");
  });
});

describe("parseRssItems", () => {
  it("XML 문자열을 RssItem 배열로 파싱 (단일/복수 item 모두)", () => {
    const items = parseRssItems(FIXTURE_XML);
    expect(items).toHaveLength(2);
    expect(items[0].title).toBe("A대학 B대학 통폐합 추진");
    expect(items[0].source).toBe("한국대학신문");
  });
  it("item 없는 빈 채널은 빈 배열", () => {
    expect(parseRssItems(`<rss><channel></channel></rss>`)).toEqual([]);
  });
});

describe("rfc2822ToIso", () => {
  it("RFC2822 pubDate를 ISO 문자열로 변환", () => {
    expect(rfc2822ToIso("Tue, 16 Jun 2026 07:00:00 GMT")).toBe(
      "2026-06-16T07:00:00.000Z",
    );
  });
  it("파싱 불가 입력은 null", () => {
    expect(rfc2822ToIso("not-a-date")).toBeNull();
  });
});

describe("stripHtml", () => {
  it("HTML 태그와 엔티티 제거 후 트림", () => {
    expect(stripHtml(`<a href="x">제목</a>&nbsp;출처`)).toBe("제목 출처");
  });
});

describe("mapRssItemsToNews", () => {
  it("RssItem[]을 news row로 정규화 + keyword 주입", () => {
    const items = parseRssItems(FIXTURE_XML);
    const rows = mapRssItemsToNews(items, "통폐합");
    expect(rows).toHaveLength(2);
    expect(rows[0]).toMatchObject({
      link: "https://news.google.com/rss/articles/AAA?oc=5",
      title: "A대학 B대학 통폐합 추진",
      source: "한국대학신문",
      published_at: "2026-06-16T07:00:00.000Z",
      keyword: "통폐합",
    });
  });
  it("link 또는 title 없는 item은 제외", () => {
    const items: RssItem[] = [
      { title: "", link: "https://x", pubDate: "", description: "", source: "" },
      { title: "제목", link: "", pubDate: "", description: "", source: "" },
    ];
    expect(mapRssItemsToNews(items, "폐교")).toHaveLength(0);
  });
});

describe("dedupeByLink", () => {
  it("동일 link 첫 건만 유지", () => {
    const rows = [
      { link: "L1", title: "a", source: null, published_at: null, summary: null, keyword: "통폐합" },
      { link: "L1", title: "b", source: null, published_at: null, summary: null, keyword: "폐교" },
      { link: "L2", title: "c", source: null, published_at: null, summary: null, keyword: "통폐합" },
    ];
    expect(dedupeByLink(rows).map((r) => r.link)).toEqual(["L1", "L2"]);
  });
});
```

  - 명령: `npm test -- src/features/automations/jobs/__tests__/news-sources.test.ts`
  - 기대 출력: 모듈 미존재로 import 실패 → `FAIL`(RED 확인).

- [ ] **Step 2: GREEN — news-sources.ts 구현** — 테스트 통과 최소 구현. `fast-xml-parser`로 파싱, 보조 순수 함수 export.

```ts
import { XMLParser } from "fast-xml-parser";

/** 수집 소스 정의 — 구글 뉴스 키워드 검색 또는 직접 RSS 피드. */
export type NewsSource =
  | { kind: "google"; keyword: string }
  | { kind: "feed"; keyword: string; url: string; label: string };

/** 구글 뉴스 RSS 키워드 검색 URL. q는 인코딩. */
export function buildGoogleNewsRssUrl(query: string): string {
  return `https://news.google.com/rss/search?q=${encodeURIComponent(
    query,
  )}&hl=ko&gl=KR&ceid=KR:ko`;
}

/**
 * 수집 키워드(스펙 §2) — 운영하며 NEWS_SOURCES 편집으로 조정.
 * 교육부/전문지 직접 피드는 placeholder — 실 URL 동작 확인 후 kind:"feed"로 추가.
 * 구글 뉴스 RSS가 전문지를 이미 집계하므로 직접 피드는 보강 성격(미존재해도 기능 성립).
 */
export const NEWS_SOURCES: NewsSource[] = [
  { kind: "google", keyword: "통폐합" },
  { kind: "google", keyword: "폐교" },
  { kind: "google", keyword: "정원감축" },
  { kind: "google", keyword: "글로컬대학" },
  { kind: "google", keyword: "구조조정" },
  // placeholder — 실 피드 URL 검증 후 활성화 (스펙 §3, §10):
  // { kind: "feed", keyword: "교육부", url: "https://www.moe.go.kr/.../rss", label: "교육부" },
];

export type RssItem = {
  title: string;
  link: string;
  pubDate: string;
  description: string;
  source: string;
};

export type NewsRow = {
  link: string;
  title: string;
  source: string | null;
  published_at: string | null;
  summary: string | null;
  keyword: string;
};

const parser = new XMLParser({ ignoreAttributes: false, trimValues: true });

/** RSS XML 문자열 → RssItem[]. 단일 item(객체)·복수 item(배열) 모두 정규화. */
export function parseRssItems(xml: string): RssItem[] {
  let doc: unknown;
  try {
    doc = parser.parse(xml);
  } catch {
    return [];
  }
  const channel = (doc as { rss?: { channel?: { item?: unknown } } })?.rss
    ?.channel;
  const raw = channel?.item;
  const arr = Array.isArray(raw) ? raw : raw ? [raw] : [];
  return arr.map((it) => {
    const o = it as Record<string, unknown>;
    const src = o.source;
    const sourceName =
      typeof src === "object" && src !== null
        ? String((src as Record<string, unknown>)["#text"] ?? "")
        : typeof src === "string"
          ? src
          : "";
    return {
      title: typeof o.title === "string" ? o.title : "",
      link: typeof o.link === "string" ? o.link : "",
      pubDate: typeof o.pubDate === "string" ? o.pubDate : "",
      description: typeof o.description === "string" ? o.description : "",
      source: sourceName,
    };
  });
}

/** RFC2822 pubDate → ISO 문자열. 파싱 불가 시 null. */
export function rfc2822ToIso(pubDate: string): string | null {
  const ms = Date.parse(pubDate);
  return Number.isNaN(ms) ? null : new Date(ms).toISOString();
}

/** HTML 태그 제거 + 기본 엔티티 디코드 + 공백 정리. */
export function stripHtml(html: string): string {
  return html
    .replace(/<[^>]*>/g, "")
    .replace(/&nbsp;/g, " ")
    .replace(/&amp;/g, "&")
    .replace(/&lt;/g, "<")
    .replace(/&gt;/g, ">")
    .replace(/&quot;/g, '"')
    .replace(/\s+/g, " ")
    .trim();
}

/** RssItem[] → news row[]. link·title 없는 항목 제외, keyword 주입. */
export function mapRssItemsToNews(items: RssItem[], keyword: string): NewsRow[] {
  return items.flatMap((it) => {
    if (!it.link || !it.title) return [];
    return [
      {
        link: it.link,
        title: it.title,
        source: it.source || null,
        published_at: rfc2822ToIso(it.pubDate),
        summary: it.description ? stripHtml(it.description) || null : null,
        keyword,
      },
    ];
  });
}

/** link 기준 dedupe — 첫 건 유지. */
export function dedupeByLink(rows: NewsRow[]): NewsRow[] {
  const map = new Map<string, NewsRow>();
  for (const r of rows) {
    if (!r.link) continue;
    if (!map.has(r.link)) map.set(r.link, r);
  }
  return Array.from(map.values());
}
```

  - 명령: `npm test -- src/features/automations/jobs/__tests__/news-sources.test.ts`
  - 기대 출력: `PASS` (전 케이스 green).

- [ ] **Step 3: REFACTOR + lint** — 중복 없음 확인. `npm run lint` → 경고 0 (`any` 미사용, `unknown` + 가드 확인).

- [ ] **Step 4: 커밋** — `git commit -m "feat(news): RSS 파싱 순수 함수 + NEWS_SOURCES (RED→GREEN)"`

**의존**: Task 1 (fast-xml-parser 설치)

---

## Task 3 — news-collect 잡 + registry 등록 (통합 — typecheck 검증)

스펙 §4. `runNewsCollect()` 5단계(소스 순회 fetch→파싱→dedupe→admin upsert onConflict link→60일 cleanup, errors[] 누적). `insights-collect.ts` 동형 골격. 외부 fetch + admin client 통합이라 단위테스트 대상은 Task 2 순수 함수로 충족 — 본 Task는 **typecheck + 수동 실행 검증**(TDD 예외, 통합 잡 검증 절차). 순수 변환 로직은 Task 2 모듈을 import.

### Files
- Create `src/features/automations/jobs/news-collect.ts`
- Modify `src/features/automations/registry.ts` (import 1줄 + 객체 1개)

### Steps

- [ ] **Step 1: news-collect.ts 구현** — `insights-collect.ts`의 errors[] 누적 + admin upsert + cleanup + `AutomationRunResult` 동형. 순수 함수는 Task 2에서 import.

```ts
import "server-only";
import { createAdminClient } from "@/lib/supabase/admin";
import {
  NEWS_SOURCES,
  buildGoogleNewsRssUrl,
  parseRssItems,
  mapRssItemsToNews,
  dedupeByLink,
  type NewsRow,
} from "./news-sources";
import type { AutomationRunResult } from "../types";

export const CLEANUP_DAYS = 60;

/** 단일 소스 fetch → news row[]. 실패 시 throw(상위에서 errors 누적). */
async function fetchSourceRows(
  source: (typeof NEWS_SOURCES)[number],
): Promise<NewsRow[]> {
  const url =
    source.kind === "google"
      ? buildGoogleNewsRssUrl(`대학 ${source.keyword}`)
      : source.url;
  const res = await fetch(url);
  if (!res.ok) {
    const body = await res.text();
    throw new Error(`${source.keyword} HTTP ${res.status}: ${body.slice(0, 200)}`);
  }
  const xml = await res.text();
  return mapRssItemsToNews(parseRssItems(xml), source.keyword);
}

export async function runNewsCollect(): Promise<AutomationRunResult> {
  const errors: string[] = [];
  const collected: NewsRow[] = [];

  // 1~3) 소스 순회 fetch → 파싱/정규화 (소스 1개 실패해도 계속)
  for (const source of NEWS_SOURCES) {
    try {
      collected.push(...(await fetchSourceRows(source)));
    } catch (e) {
      errors.push(e instanceof Error ? e.message : String(e));
    }
  }

  // dedupe (link 기준)
  const rows = dedupeByLink(collected);
  if (rows.length === 0) {
    return {
      ok: errors.length === 0,
      message: errors.length
        ? `수집 실패: ${errors.length}건`
        : "신규 뉴스가 없습니다.",
      details: { collected: 0, errors: errors.length },
    };
  }

  const errorSuffix = () => (errors.length ? ` (${errors.length}건 오류)` : "");

  // 4) admin upsert (onConflict link)
  const supabase = createAdminClient();
  const { data, error } = await supabase
    .from("news")
    .upsert(rows, { onConflict: "link", ignoreDuplicates: false })
    .select("link");
  if (error) {
    return { ok: false, message: `upsert 실패: ${error.message}` };
  }

  // 5) 60일 지난 행 cleanup
  const cleanupCutoff = new Date(
    Date.now() - CLEANUP_DAYS * 24 * 60 * 60 * 1000,
  ).toISOString();
  const { data: deleted, error: cleanupError } = await supabase
    .from("news")
    .delete()
    .lt("collected_at", cleanupCutoff)
    .select("id");
  if (cleanupError) {
    errors.push(`cleanup 실패: ${cleanupError.message}`);
  }

  return {
    ok: true,
    message: `${data?.length ?? 0}건 적재, ${deleted?.length ?? 0}건 정리${errorSuffix()}`,
    details: {
      upserted: data?.length ?? 0,
      cleaned: deleted?.length ?? 0,
      errors: errors.length,
    },
  };
}
```

- [ ] **Step 2: registry 등록** — `registry.ts` import 1줄 + `AUTOMATION_JOBS` 배열에 객체 1개(`insights-collect` 항목 다음에 삽입).

import 추가 (line 3 `runInsightsCollect` import 다음):
```ts
import { runNewsCollect } from "./jobs/news-collect";
```

배열 객체 추가 (`insights-collect` 엔트리 다음):
```ts
  {
    id: "news-collect",
    label: "운영부 뉴스 수집",
    description:
      "대학 관련 뉴스(통폐합·폐교·정원감축 등)를 멀티소스 RSS로 수집해 운영부 뉴스 페이지에 적재합니다.",
    scheduleInfo: "30분 주기 자동 (cron-job.org)",
    cooldownMinutes: 30,
    run: runNewsCollect,
  },
```

- [ ] **Step 3: 검증** — `npm run typecheck && npm run lint`.
  - 기대 출력: 에러/경고 0. `getJob("news-collect")`가 registry에서 resolve(타입상 보장 — `AUTOMATION_JOBS` 항목 추가).
  - (선택, 실 env + 적용된 테이블 있을 때) 수동 실행 검증: 자동화 UI '지금 실행' 또는 `curl -X POST "$BASE/api/automations/run?jobId=news-collect" -H "Authorization: Bearer $CRON_SECRET"` → `{ ok:true, message:"N건 적재..." }`. `news` 테이블 count 증가 확인.

- [ ] **Step 4: 커밋** — `git commit -m "feat(news): news-collect 잡 + registry 등록"`

**의존**: Task 2 (news-sources), Task 1 (테이블)

---

## Task 4 — news feature (schemas + queries, RED 먼저)

스펙 §6. `newsRowSchema`(zod) + `listNews()`(authenticated RLS, published_at desc limit 100, zod per-row). `insight-videos/{schemas,queries}.ts` 차용. schema는 TDD 적용(RED→GREEN), queries는 SSR fetch(TDD 예외 — typecheck 검증).

### Files
- Create `src/features/news/__tests__/schemas.test.ts`
- Create `src/features/news/schemas.ts`
- Create `src/features/news/queries.ts`

### Steps

- [ ] **Step 1: RED — 스키마 테스트** — `schemas.test.ts`. 유효 row 통과 + link/title 필수 + nullable 필드 확인.

```ts
import { describe, it, expect } from "vitest";
import { newsRowSchema } from "../schemas";

const validRow = {
  id: "11111111-1111-4111-8111-111111111111",
  link: "https://news.google.com/rss/articles/AAA",
  title: "A대학 B대학 통폐합 추진",
  source: "한국대학신문",
  published_at: "2026-06-16T07:00:00.000Z",
  summary: "A대학 B대학 통폐합 추진",
  keyword: "통폐합",
  collected_at: "2026-06-23T00:00:00.000Z",
};

describe("newsRowSchema", () => {
  it("유효 row 통과", () => {
    expect(newsRowSchema.safeParse(validRow).success).toBe(true);
  });
  it("link 누락 시 fail", () => {
    expect(newsRowSchema.safeParse({ ...validRow, link: "" }).success).toBe(
      false,
    );
  });
  it("title 누락 시 fail", () => {
    expect(newsRowSchema.safeParse({ ...validRow, title: "" }).success).toBe(
      false,
    );
  });
  it("nullable 필드(source/published_at/summary/keyword) null 허용", () => {
    expect(
      newsRowSchema.safeParse({
        ...validRow,
        source: null,
        published_at: null,
        summary: null,
        keyword: null,
      }).success,
    ).toBe(true);
  });
});
```

  - 명령: `npm test -- src/features/news/__tests__/schemas.test.ts`
  - 기대 출력: import 실패 → `FAIL`(RED).

- [ ] **Step 2: GREEN — schemas.ts 구현** — `insightVideoRowSchema` 차용. nullable 필드는 `.nullable()`.

```ts
import { z } from "zod";

export const newsRowSchema = z.object({
  id: z.string().uuid(),
  link: z.string().min(1),
  title: z.string().min(1),
  source: z.string().nullable().optional(),
  published_at: z.string().nullable().optional(),
  summary: z.string().nullable().optional(),
  keyword: z.string().nullable().optional(),
  collected_at: z.string(),
});

export type NewsRow = z.infer<typeof newsRowSchema>;
```

  - 명령: `npm test -- src/features/news/__tests__/schemas.test.ts`
  - 기대 출력: `PASS`.

- [ ] **Step 3: queries.ts 구현 (SSR fetch — TDD 예외, typecheck 검증)** — `listInsightVideos` 패턴 차용. published_at desc, limit 100, zod per-row. **server-only**.

```ts
import "server-only";
import { createClient } from "@/lib/supabase/server";
import { newsRowSchema, type NewsRow } from "./schemas";

/**
 * 운영부 뉴스 목록 fetch (RSC).
 * RLS: authenticated → 모든 row read 허용 (운영부 공개 정책).
 * 정렬: published_at desc (최신 기사 우선) — null published_at은 후순위.
 * 최대 100건.
 */
export async function listNews(): Promise<NewsRow[]> {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("news")
    .select("*")
    .order("published_at", { ascending: false, nullsFirst: false })
    .limit(100);

  if (error) {
    console.error("[listNews] supabase error:", error);
    return [];
  }

  const parsed: NewsRow[] = [];
  for (const row of data ?? []) {
    const r = newsRowSchema.safeParse(row);
    if (r.success) parsed.push(r.data);
    else console.error("[listNews] zod parse fail:", r.error.issues, "row:", row);
  }
  return parsed;
}
```

  - 명령: `npm run typecheck`
  - 기대 출력: 에러 0 (`news/` 신규 모듈 타입 통과).

- [ ] **Step 4: 커밋** — `git commit -m "feat(news): zod schema + listNews query (RED→GREEN)"`

**의존**: Task 1 (테이블)

---

## Task 5 — 사이드바 메뉴 + 페이지 메타 (slug news)

스펙 §6. '개요' group의 운영부 달력(`schedule`) item 직후에 1줄 + 페이지 메타 1항목. 타입/설정 변경 — TDD 예외, `_data.test.ts`(존재 시) + 빌드로 검증.

### Files
- Modify `src/app/dashboard/_data.ts` (line 42~49 — '운영부 달력' schedule item 직후)
- Modify `src/app/dashboard/_data/page-meta-config.ts` (PAGE_META에 `news` 항목)

### Steps

- [ ] **Step 1: 사이드바 item 추가** — `_data.ts` '개요' section entries에서 `schedule`(운영부 달력) item 다음에 삽입.

```ts
      {
        kind: "item",
        ico: "▤",
        label: "운영부 뉴스",
        count: "",
        slug: "news",
        pattern: "list",
      },
```

- [ ] **Step 2: 페이지 메타 추가** — `page-meta-config.ts` `PAGE_META`에 `news` 항목.

```ts
  news: {
    headline: { accent: "개요", title: "운영부 뉴스" },
    description:
      "대학 통폐합·폐교·정원감축 등 운영부 관련 뉴스를 자동 수집해 최신순으로 모아봅니다.",
  },
```

- [ ] **Step 3: 검증** — `npm run typecheck` + (`src/app/dashboard/__tests__/_data.test.ts` 존재 시) `npm test -- src/app/dashboard/__tests__/_data.test.ts`. `findSidebarMeta("news")`가 `{ pattern: "list", ... }` 반환.
  - 기대 출력: typecheck 에러 0, 테스트 `PASS`.

- [ ] **Step 4: 커밋** — `git commit -m "feat(news): 사이드바 메뉴 + 페이지 메타 (slug news)"`

**의존**: 없음 (페이지 Task 7 전에 slug 등록 필요 — 7의 선행)

---

## Task 6 — news variant (View/Table/filters) + ListRow 필드 + registry/types

스펙 §6. variant 폴더(View 읽기전용 + Table + filters) + registry 1줄 + types union 1줄 + ListRow `news*` 옵셔널 필드. View는 제목·출처·날짜·요약 + 원문 링크 버튼. `worklog/View.tsx`(View-only) + `mailbox/Table.tsx` 차용. **디자인 토큰 준수 — 하드코딩 색상 금지**(기존 variant의 `text-ink`/`text-muted`/`bg-washi-raised`/`border-line` 등 토큰 클래스만 사용). ListRow 필드 추가는 타입 변경(TDD 예외) — typecheck 검증.

### Files
- Modify `src/app/dashboard/_components/patterns/ListPattern.tsx` (`ListRow` 타입 끝 `};` 직전 — mail* 블록 line 510 뒤)
- Modify `src/app/dashboard/_components/inspector/list-variants/types.ts` (`Variant` union 끝 `| "mailbox"` 뒤)
- Modify `src/app/dashboard/_components/inspector/list-variants/registry.ts` (import + `news:` 엔트리)
- Create `src/app/dashboard/_components/inspector/list-variants/news/View.tsx`
- Create `src/app/dashboard/_components/inspector/list-variants/news/Table.tsx`
- Create `src/app/dashboard/_components/inspector/list-variants/news/filters.ts`

### Steps

- [ ] **Step 1: ListRow에 news* 필드 추가** — `ListPattern.tsx` ListRow 타입 끝(mailbox `mailDraftStatus` 블록 다음, `};` 직전).

```ts
  /** news variant — 원문(또는 구글 리다이렉트) 링크 */
  newsLink?: string;
  /** news — 언론사명 */
  newsSource?: string | null;
  /** news — 기사 게시일 (ISO) */
  newsPublishedAt?: string | null;
  /** news — 요약 스니펫 (description HTML 제거) */
  newsSummary?: string | null;
  /** news — 수집에 사용된 검색 키워드 */
  newsKeyword?: string | null;
```

- [ ] **Step 2: types.ts Variant union + filters.ts** — union 끝에 `| "news"` 추가. filter는 키워드 칩(스펙 §6) — 5개 키워드 옵션. `Filter` 타입은 `ListPattern`에서 import.

`types.ts` (union 마지막 `| "mailbox";` → ):
```ts
  | "mailbox"
  | "news";
```

`news/filters.ts`:
```ts
import type { Filter } from "../../../patterns/ListPattern";

// news: 신규 생성 흐름 없음 (news-collect 잡이 적재). 키워드 chip으로 필터.
// Filter union에 키워드 값이 없으면 빈 배열로 두고 ScopeChips 미사용 — 실 Filter union
// 확장이 필요하면 ListPattern Filter 타입에 키워드 값을 먼저 추가한다(별도 결정).
export const NEWS_FILTERS: { value: Filter; label: string }[] = [];
```

> 주의: 키워드 chip을 실제 활성화하려면 `ListPattern.tsx`의 `Filter` union(line 525)에 키워드 값 추가가 선행돼야 한다. 1차는 `mailbox`/`worklog`처럼 빈 filter(전체 표시)로 출시하고, 키워드 필터는 Filter union 확장과 함께 후속 처리. 본 Task 범위는 빈 배열.

- [ ] **Step 3: Table.tsx** — `mailbox/Table.tsx` 차용. 출처 + 제목 + 날짜 3열. 하드코딩 색상 금지(토큰 클래스). 빈 상태 행 포함.

```tsx
"use client";

import type { ListRow } from "../../../patterns/ListPattern";

type Props = {
  rows: ListRow[];
  selectedId: string | null;
  onSelect: (row: ListRow) => void;
};

function formatDate(iso?: string | null): string {
  if (!iso) return "-";
  return new Intl.DateTimeFormat("ko-KR", {
    timeZone: "Asia/Seoul",
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
  }).format(new Date(iso));
}

export function NewsTable({ rows, selectedId, onSelect }: Props) {
  return (
    <table className="w-full text-sm">
      <thead>
        <tr className="border-b border-line text-left text-xs uppercase tracking-[0.06em] text-muted">
          <th className="px-3 py-2">출처</th>
          <th className="px-3 py-2">제목</th>
          <th className="px-3 py-2">게시일</th>
        </tr>
      </thead>
      <tbody>
        {rows.length === 0 ? (
          <tr>
            <td colSpan={3} className="px-3 py-6 text-center text-muted">
              수집된 뉴스 없음
            </td>
          </tr>
        ) : (
          rows.map((row) => (
            <tr
              key={row.id}
              onClick={() => onSelect(row)}
              className={`cursor-pointer border-b border-line-soft hover:bg-washi-raised ${
                selectedId === row.id ? "bg-washi-raised" : ""
              }`}
            >
              <td className="px-3 py-2 text-xs text-muted">
                {row.newsSource || "-"}
              </td>
              <td className="px-3 py-2 font-medium text-ink">{row.name}</td>
              <td className="px-3 py-2 text-sm text-ink-soft">
                {formatDate(row.newsPublishedAt)}
              </td>
            </tr>
          ))
        )}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 4: View.tsx** — `worklog/View.tsx`(View-only) 차용. 제목·출처·게시일·요약 표시 + 원문 링크. 외부 링크는 `target="_blank" rel="noopener noreferrer"`. 토큰 클래스만.

```tsx
import { Section, DefList, Divider } from "../shared";
import type { ViewProps } from "../types";

function formatDate(iso?: string | null): string {
  if (!iso) return "-";
  return new Intl.DateTimeFormat("ko-KR", {
    timeZone: "Asia/Seoul",
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  }).format(new Date(iso));
}

export function NewsView({ row }: ViewProps) {
  return (
    <div className="space-y-6">
      <Section title="기사">
        <DefList
          items={[
            { term: "제목", desc: row.name || "-" },
            { term: "출처", desc: row.newsSource || "-" },
            { term: "게시일", desc: <span>{formatDate(row.newsPublishedAt)}</span> },
            { term: "키워드", desc: row.newsKeyword || "-" },
          ]}
        />
      </Section>

      <Divider />

      <Section title="요약">
        <p className="text-sm leading-relaxed text-ink-soft">
          {row.newsSummary || "요약이 없습니다."}
        </p>
      </Section>

      {row.newsLink ? (
        <a
          href={row.newsLink}
          target="_blank"
          rel="noopener noreferrer"
          className="inline-block border border-line px-3 py-1.5 text-sm text-ink hover:bg-ink hover:text-cream"
        >
          원문 보기 ↗
        </a>
      ) : null}
    </div>
  );
}
```

> 버튼 호버는 메모리 `button-hover-black-rule`(`hover:bg-ink hover:text-cream`)를 따른다. `shared`의 `Section/DefList/Divider` 시그니처는 `worklog/View.tsx` import와 동일.

- [ ] **Step 5: registry 등록** — `registry.ts` import 3줄 + `news:` 엔트리. View-only(EditForm/blank 없음, mailbox/worklog 패턴).

import 추가:
```ts
import { NewsView } from "./news/View";
import { NewsTable } from "./news/Table";
import { NEWS_FILTERS } from "./news/filters";
```

`variantRegistry` 객체에 추가(`mailbox:` 엔트리 다음):
```ts
  news: {
    // 읽기 전용 — 수집 뉴스 (편집/신규 없음). 잡이 적재.
    View: NewsView,
    Table: NewsTable,
    Filters: NEWS_FILTERS,
  },
```

- [ ] **Step 6: 검증** — `npm run typecheck && npm run lint`.
  - 기대 출력: 에러/경고 0. `variantRegistry`가 `Record<Variant, RegistryEntry>` satisfies 통과(union에 `news` 추가됨). design-lint hook이 Write 시 하드코딩 색상 미검출.

- [ ] **Step 7: 커밋** — `git commit -m "feat(news): news variant(View/Table/filters) + ListRow 필드 + registry"`

**의존**: Task 5 권장(slug 등록). registry/types는 독립.

---

## Task 7 — 페이지 + row-mapper (slug news, RED 먼저 — row-mapper)

스펙 §6. `src/app/dashboard/news/page.tsx`(requireMenu→listNews→ListPattern variant="news", autoRefresh) + `_row-mapper.ts`(newsRow→ListRow). row-mapper는 순수 함수 — RED→GREEN. 페이지 SSR은 TDD 예외(typecheck+빌드 검증). `mailbox/page.tsx` + `_row-mapper.test.ts` 차용.

### Files
- Create `src/app/dashboard/news/__tests__/_row-mapper.test.ts`
- Create `src/app/dashboard/news/_row-mapper.ts`
- Create `src/app/dashboard/news/page.tsx`

### Steps

- [ ] **Step 1: RED — row-mapper 테스트** — `_row-mapper.test.ts`. newsRow→ListRow 필드 매핑 + 제목/링크/출처/날짜/키워드.

```ts
import { describe, it, expect } from "vitest";
import { newsRowToListRow } from "../_row-mapper";
import type { NewsRow } from "@/features/news/schemas";

const newsRow: NewsRow = {
  id: "11111111-1111-4111-8111-111111111111",
  link: "https://news.google.com/rss/articles/AAA",
  title: "A대학 B대학 통폐합 추진",
  source: "한국대학신문",
  published_at: "2026-06-16T07:00:00.000Z",
  summary: "A대학과 B대학이 통폐합을 추진한다.",
  keyword: "통폐합",
  collected_at: "2026-06-23T00:00:00.000Z",
};

describe("newsRowToListRow", () => {
  it("news* 필드 매핑 + name=제목 + status=active", () => {
    const row = newsRowToListRow(newsRow);
    expect(row.id).toBe(newsRow.id);
    expect(row.name).toBe("A대학 B대학 통폐합 추진");
    expect(row.status).toBe("active");
    expect(row.newsLink).toBe("https://news.google.com/rss/articles/AAA");
    expect(row.newsSource).toBe("한국대학신문");
    expect(row.newsPublishedAt).toBe("2026-06-16T07:00:00.000Z");
    expect(row.newsSummary).toBe("A대학과 B대학이 통폐합을 추진한다.");
    expect(row.newsKeyword).toBe("통폐합");
  });

  it("nullable 필드 null 시 owner/meta 폴백", () => {
    const row = newsRowToListRow({
      ...newsRow,
      source: null,
      published_at: null,
      summary: null,
      keyword: null,
    });
    expect(row.newsSource).toBeNull();
    expect(row.newsSummary).toBeNull();
    expect(row.name).toBe("A대학 B대학 통폐합 추진");
  });
});
```

  - 명령: `npm test -- src/app/dashboard/news/__tests__/_row-mapper.test.ts`
  - 기대 출력: import 실패 → `FAIL`(RED).

- [ ] **Step 2: GREEN — _row-mapper.ts 구현** — `mailboxEntryToListRow` 패턴 차용. `owner`/`meta`는 출처/게시일 derive.

```ts
import type { ListRow } from "../_components/patterns/ListPattern";
import type { NewsRow } from "@/features/news/schemas";

/** news row → ListRow (variant="news"). 읽기 전용 — status는 항상 active. */
export function newsRowToListRow(news: NewsRow): ListRow {
  return {
    id: news.id,
    name: news.title,
    status: "active",
    owner: news.source ?? "",
    meta: news.keyword ?? undefined,
    newsLink: news.link,
    newsSource: news.source ?? null,
    newsPublishedAt: news.published_at ?? null,
    newsSummary: news.summary ?? null,
    newsKeyword: news.keyword ?? null,
  };
}
```

  - 명령: `npm test -- src/app/dashboard/news/__tests__/_row-mapper.test.ts`
  - 기대 출력: `PASS`.

- [ ] **Step 3: page.tsx 구현 (SSR — TDD 예외, typecheck+빌드 검증)** — `mailbox/page.tsx` 차용. readOnly + liveData + autoRefresh. 발송 없음(읽기 전용)이라 onMail* 콜백·extraActions 없음.

```tsx
import { findSidebarMeta } from "../_data";
import { resolvePageMeta } from "../_data/page-meta-derive";
import { PageHeader } from "../_components/page-header/PageHeader";
import { ListPattern } from "../_components/patterns/ListPattern";
import type { ListRow } from "../_components/patterns/ListPattern";
import { requireMenu } from "@/features/auth/menu-guard";
import { listNews } from "@/features/news/queries";
import { newsRowToListRow } from "./_row-mapper";

export default async function NewsPage() {
  const slug = "news";
  await requireMenu(slug);

  const meta = findSidebarMeta(slug);
  if (!meta) return null;
  const pathname = `/dashboard/${slug}`;

  const news = await listNews();
  const rows: ListRow[] = news.map(newsRowToListRow);
  const config = resolvePageMeta(slug, meta, news.length);

  const header = (
    <div key="news-header">
      <PageHeader
        pathname={pathname}
        meta={config.meta}
        headline={config.headline}
        description={config.description}
        autoRefresh
      />
    </div>
  );

  return (
    <ListPattern
      title={meta.label}
      data={{ rows }}
      header={header}
      variant="news"
      readOnly
      liveData
    />
  );
}
```

  - 명령: `npm run typecheck && npm run build`
  - 기대 출력: typecheck 에러 0. build 성공(`/dashboard/news` 라우트 prerial). **빌드 시 `NODE_ENV` shell leak 주의** — `unset NODE_ENV` 또는 `NODE_ENV=production`(CLAUDE.md 빌드 운영 메모).

- [ ] **Step 4: 커밋** — `git commit -m "feat(news): /dashboard/news 페이지 + row-mapper (RED→GREEN)"`

**의존**: Task 4(listNews), Task 5(slug 등록), Task 6(variant "news" + ListRow 필드)

---

## 운영 선행조건 (코드 외 — 본 계획 범위 밖)

스펙 §9. 코드 머지 후 운영 단계에서 수행:

- DB 마이그 적용 (`news` 테이블 + RLS) — 메모리 `db-migration-apply` 절차
- cron-job.org에 `POST /api/automations/run?jobId=news-collect` 잡 등록 (30분 주기, `Authorization: Bearer CRON_SECRET`)
- 최초 1회 수동 실행(자동화 UI '지금 실행')로 수집 동작 검증
- 교육부·전문지 직접 RSS 피드 URL 실제 존재·동작 검증 후 `NEWS_SOURCES`에 `kind:"feed"` 추가(스펙 §10). 미존재해도 구글 뉴스 RSS가 핵심이라 기능 성립

---

## 스펙 커버리지 self-review

| 스펙 항목 | 커버 Task | 비고 |
|---|---|---|
| §2 멀티소스 RSS / 키워드 5개 | Task 2 (NEWS_SOURCES) | 구글 뉴스 5 키워드 + feed placeholder |
| §2 `fast-xml-parser` 추가 | Task 1 Step 1 | `npm i` + parseRssItems 사용(Task 2) |
| §3 구글 뉴스 RSS URL/필드 | Task 2 (buildGoogleNewsRssUrl/parseRssItems) | source·pubDate·description 추출 + 픽스처 테스트 |
| §3 교육부·전문지 placeholder | Task 2 (NEWS_SOURCES 주석 feed) | 실 URL 검증은 운영 선행조건으로 분리(§10) |
| §4 runNewsCollect 5단계 | Task 3 | fetch→파싱→dedupe→upsert(onConflict link)→60일 cleanup, errors[] |
| §4 registry 1줄 등록 | Task 3 Step 2 | import 1줄 + 객체 1개, cron/이력/UI 자동 |
| §4 순수 함수 export+단위테스트 | Task 2 | parse/map/dedupe/rfc2822ToIso/stripHtml RED→GREEN |
| §5 news 테이블 (link unique) | Task 1 Step 2 | published_at/keyword 인덱스 |
| §5 RLS = insight_videos 복사 | Task 1 Step 3 | select authenticated / write service_role / GRANT |
| §6 사이드바 개요 schedule 직후 | Task 5 Step 1 | `_data.ts` 1줄 |
| §6 page.tsx requireMenu→listNews→ListPattern | Task 7 Step 3 | readOnly + autoRefresh |
| §6 feature schemas/queries | Task 4 | newsRowSchema + listNews(published_at desc 100) zod per-row |
| §6 variant View/Table/filters | Task 6 | View 제목·출처·날짜·요약+원문링크 / Table 출처·제목·날짜 |
| §6 registry 1줄 + types union 1줄 + ListRow news* | Task 6 | View-only 엔트리 |
| §7 신규 의존성 fast-xml-parser | Task 1 | — |
| §9 운영 선행조건 | 운영 선행조건 단락 | 마이그 적용·cron 등록·수동 검증·feed URL 검증 |
| §10 구글 link=리다이렉트 / summary 빈약 | Task 2 (mapRssItemsToNews) + Task 6 View | link 그대로 dedupe·원문 이동, summary는 stripHtml 스니펫 |

**미반영(의도적, 스펙 허용):**
- `news_blocklist`(선택, §5) — insight `excludeBlocked` 대응. 1차 미포함(스펙 "선택"). 필요 시 후속 Task로 `excludeBlocked` + 테이블 추가.
- 키워드 chip 필터 실제 동작(§6) — `ListPattern`의 `Filter` union 확장 선행 필요. 1차는 빈 filter(전체 표시), Task 6 Step 2 주의 박스에 명시.

**리스크:**
- 구글 뉴스 RSS `<source>` 구조가 fast-xml-parser 옵션(`ignoreAttributes`)에 따라 `#text` 키로 파싱됨 — Task 2 parseRssItems가 객체/문자열 양쪽 처리. 픽스처 테스트로 회귀 방어. 실제 피드 구조가 다르면 Task 3 수동 실행 시 첫 검증에서 드러남.
- `published_at` null 정렬 — `nullsFirst: false`로 null 후순위(Task 4). 구글 뉴스는 pubDate 항상 존재해 실질 영향 적음.
- ListPattern `Filter` union/`ListRow` 타입은 다수 variant 공유 — Task 6 추가 필드는 모두 optional이라 기존 variant 무영향(typecheck로 보장).
